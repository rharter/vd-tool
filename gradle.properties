org.gradle.caching=true
# The `render` task's cross-project configuration in root build.gradle.kts is
# only entered when -Pinput is set, so everyday builds (e.g. :frontend:installDist)
# fingerprint cleanly and reuse the cache. The `render` task itself won't.
org.gradle.configuration-cache=true
